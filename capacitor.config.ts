import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'delvify_assignment',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
